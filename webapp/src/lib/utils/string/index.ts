// Export all string operations
export { stringCaseOperations } from './case';

// Import for local use
import { stringCaseOperations } from './case';

// Re-export for convenience
export const stringUtils = {
  case: stringCaseOperations,
};